server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:

          uri: https://github.com/JhonyAMP/JMP-MICROSERVICES.git
          searchPaths: config-data
          default-label: main
  security:
    user:

      name: jhonymp
      password: root
